import React from 'react';
import './ArticlePreview.css';

export default function ArticlePreview({ title, publishDate, writtenBy, previewImage }) {
  console.log(title)
  return (
    <h1>Dog Boy</h1>
  )
}